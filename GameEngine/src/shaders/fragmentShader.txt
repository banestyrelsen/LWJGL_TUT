#version 400 core

in vec2 pass_textureCoords;
out vec4 out_Color;

uniform sampler2D textureSampler;

void main(void) {
	// Executes for each pixel 
	out_Color = texture(textureSampler, pass_textureCoords); // Returns color of pixel at these coordinates
}